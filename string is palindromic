int main() {  
    char* words[] = {"hello", "world", "madam", "abcba", "def"};  
    int wordsSize = sizeof(words) / sizeof(words);  
  
    char* result = firstPalindrome(words, wordsSize);  
    printf("%s\n", result); // Output: "madam"  
  
    return 0;  
}  
